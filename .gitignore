node_modules/
# Ignore .env and all variants (.env.local, .env.production, ...) at any depth,
# but keep the placeholder template tracked.
.env*
!.env.example
*.log
client/dist/

# Contains a stale plaintext MySQL password; keep it out of git history.
scripts/reset-mysql-password.ps1
